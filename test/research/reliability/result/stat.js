
/**
 * 统计逻辑：
 *
 * 按照各 1 / 3 的概率加载 test-a.html, test-r.html, test-s.html
 * use 前，打上 x-init 比较
 *
 * 数据分析：
 *  1. x-init: 表示 use 前无错的进入总值，理论上 a-init, r-init, s-init 应该尽量相等
 *  2. x-[a-d_]{6}: 这是加载器捕获的有效值
 *  3. 有效值 / 总值 越高，说明 正常加载 + 错误捕获 能力越强
 *
 * 对于有效值，再进一步分析：
 *  1. 完全正确的：x-abcd
 *  2. 有缺失但位置正确的，如：x-a_cde_
 *  3. 位置错误的，比如 x-bacdef
 *
 * 最后可以看两个比率：
 *  1. 完全正确的 / 总值
 *  2. （完全正确的 + 有缺失但位置正确的） / 总值
 *
 */

